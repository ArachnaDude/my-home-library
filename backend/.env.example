DB_NAME=home_library
DB_USER=matt
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

# Used by SQLAlchemy in db.py (optional if DB_* vars above are set)
DATABASE_URL=postgresql+psycopg://matt@localhost:5432/home_library
