DB_NAME=home_library
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Docker Compose sets DB_HOST=db for the backend service automatically.

# Optional — deploy platforms often provide this instead of DB_* vars above
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/home_library
